import { HomeLabPage } from "@/components/home/HomeLabPage";

/**
 * LOGIC_TOUCH index (Home):
 * - home.session-gate
 * - home.persona
 * - home.guest-data
 * - home.guest-login-panel
 * - home.feed
 * - home.sidebar-modules
 * - home.follow-org
 * - home.profile-completeness
 * - home.compose
 * - home.feed-boost
 *
 * Chi tiết: ../../LOGIC_TOUCHPOINTS.md
 */
export default function HomePage() {
  return <HomeLabPage />;
}
