---
description: Chốt quyết định CINs — options, recommendation, ghi DECISIONS hoặc docs/ops/decisions
argument-hint: "<quyết định cần chốt>"
---

# /cins:decide

Chạy skill **cins-decide**.

```
/cins:decide $ARGUMENTS
```

Restate → options → recommend → hỏi confirm path ghi → ghi file → optional Graphiti `[CINS]` Decision.
